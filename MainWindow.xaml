<Window x:Class="GRA2D.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GRA2D"
        mc:Ignorable="d"
        WindowState="Maximized" WindowStyle="None"
        Title="Menu_Window" Height="1080"  Width="1920">
    <Window.Resources>
        <Style TargetType="Button" x:Key="Round_Corners">
            <Setter Property="Margin" Value="70"></Setter>
            <Setter Property="Width" Value="400"></Setter>
            <Setter Property="Height" Value="100"></Setter>
            <Setter Property="Background" Value="DodgerBlue"></Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="30"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter 
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                            </ContentPresenter>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            
        </Style>
    </Window.Resources>
    
    <Grid Background="#FFB2D8B2">
        <Border Background="LightSkyBlue" CornerRadius="100" Padding="20"
                Width="600" Height="800" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Button Style="{StaticResource Round_Corners }" Content="Start"  Foreground="White" FontWeight="Bold" BorderThickness="0" FontSize="30" ></Button>
                <Button Style="{StaticResource Round_Corners }" Content="Ustawienia" Foreground="White" FontWeight="Bold" BorderThickness="0" FontSize="30" ></Button>
                <Button Style="{StaticResource Round_Corners }" Content="Wyjdź"  Foreground="White" FontWeight="Bold" BorderThickness="0" FontSize="30" ></Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
